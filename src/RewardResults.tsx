import {Disclosure} from './UI';
import rows from './reward-results.json';
import './logo.css';
const base='/reports/reward-followup/';
export function RewardResults(){return <div className="logo-results reward-results">
 <h3>How to read the chart</h3><p>Each bar counts how many times the fly reached its taught picture first. A taller bar means more visits, not more happiness. Each group had 12 chances.</p><p>Green means reward with the picture. Brown means reward at a different time. Blue means learning switched off. Read the three sets from left to right: treats stopped, more views without treats, then a longer walk.</p><figure className="research-figure"><a href={`${base}results.png`} target="_blank" rel="noopener noreferrer"><img loading="lazy" src={`${base}results.png`} width="1980" height="900" alt="Trained logo approached first: paired, unpaired, learning disabled respectively: 3, 1, 2 after reward removal; 1, 2, 2 after unrewarded exposure; 1, 2, 1 with greater travel distance."/></a><figcaption>The green bar starts a little higher, but does not stay higher. “Paired” means together; “unpaired” means separately; “frozen” means learning off. A score of 3/12 means three visits in twelve chances.</figcaption></figure>
 <div className="reward-table" tabIndex={0} role="region" aria-label="Reward follow-up results"><table><caption>First approach to the trained logo</caption><thead><tr><th scope="col">Test</th><th scope="col">Reward with logo</th><th scope="col">Reward separately</th><th scope="col">Learning off</th></tr></thead><tbody>{rows.map(row=><tr key={row.label}><th scope="row">{row.label}</th>{row.counts.map((v,i)=><td key={i}>{v}/12</td>)}</tr>)}</tbody></table></div>
 <Disclosure title="Did it stay near the picture longer?"><p>We also added up how long the fly stayed near its taught picture. More seconds means more time nearby. It does not tell us why the fly stayed.</p><p>After repeated views without reward, the reward-with-picture group stayed for less time than both other groups. In the longer-walk test it stayed a little longer, but did not reach the picture first more often than both groups. That is not a clear, lasting preference.</p><div className="reward-table" tabIndex={0} role="region" aria-label="Time near trained logo"><table><caption>Total seconds across 12 tests in each group</caption><thead><tr><th scope="col">Test</th><th scope="col">Reward with logo</th><th scope="col">Reward separately</th><th scope="col">Learning off</th></tr></thead><tbody>{rows.map(row=><tr key={row.label}><th scope="row">{row.label}</th>{row.dwell.map((v,i)=><td key={i}>{v.toFixed(2)} s</td>)}</tr>)}</tbody></table></div></Disclosure>
 <Disclosure title="What do the brain charts show?"><p>These charts show the pretend reward reaching the computer brain during teaching. Read from left to right as time passes. The shaded stripes mark when we sent the reward. The green line shows how often the watched brain cells sent electrical signals.</p><p>A higher line means more signals, not more happiness. PAM11 is just the name of the group of cells we watched. Hz means signals per second. The three rows show reward with the picture, reward separately, and learning off.</p><p>The lines rise even when learning is off. That is why a rising brain signal alone cannot show that the fly learned to love a picture. The walking tests above tell us whether its choices changed.</p>{['instagram','google','whatsapp','youtube'].map(logo=><div key={logo}><h3>{logo==='youtube'?'YouTube':logo==='whatsapp'?'WhatsApp':logo[0].toUpperCase()+logo.slice(1)}</h3>{[101,102,103].map(seed=><figure className="research-figure" key={seed}><a href={`${base}training-plots/training-${logo}-${seed}.png`} target="_blank" rel="noopener noreferrer"><img loading="lazy" src={`${base}training-plots/training-${logo}-${seed}.png`} alt={`${logo} training measurements, starting setup ${seed}`}/></a><figcaption>{logo} · setup {seed}. Shading: reward sent. Green line: recorded brain signals. Tap to enlarge.</figcaption></figure>)}</div>)}</Disclosure>
 <Disclosure title="How we checked the records"><p>We checked all 108 test rows against the numbers shown here, and checked 87 saved files against their original fingerprints. The supplied checks also confirmed that no rewards were given during the walking tests and that connections stayed unchanged when learning was off.</p><p>Ten walks stopped a little early because the fly left the test area. We kept those results. These checks help catch recording mistakes; they do not prove the computer behaves like a real fly.</p></Disclosure><p className="reward-downloads">{[['Full report','report.md'],['All 108 test results','probe-results.csv'],['Method and settings','protocol.json'],['Recorded checks','verification.json'],['File checksums','manifest.json']].map(([label,path])=><a key={path} href={base+path} download>{label}</a>)}</p>
 </div>}
