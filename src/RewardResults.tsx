import {Disclosure} from './UI';
import rows from './reward-results.json';
const base='/reports/reward-followup/';
export function RewardResults(){return <div className="logo-results reward-results">
 <figure className="research-figure"><a href={`${base}results.png`} target="_blank" rel="noopener noreferrer"><img loading="lazy" src={`${base}results.png`} width="1980" height="900" alt="Trained logo approached first: paired, unpaired, learning disabled respectively: 3, 1, 2 after reward removal; 1, 2, 2 after unrewarded exposure; 1, 2, 1 with greater travel distance."/></a><figcaption>The recorded follow-up figure. Paired means reward with the logo; unpaired means reward separately; frozen means learning disabled. Each bar counts scenes where the trained logo was approached first, out of 12.</figcaption></figure>
 <div className="reward-table" tabIndex={0} role="region" aria-label="Reward follow-up results"><table><caption>First approach to the trained logo</caption><thead><tr><th scope="col">Test</th><th scope="col">Reward with logo</th><th scope="col">Reward separately</th><th scope="col">Learning off</th></tr></thead><tbody>{rows.map(row=><tr key={row.label}><th scope="row">{row.label}</th>{row.counts.map((v,i)=><td key={i}>{v}/12</td>)}</tr>)}</tbody></table></div>
 <Disclosure title="Time spent near the trained logo"><div className="reward-table" tabIndex={0} role="region" aria-label="Time near trained logo"><table><caption>Total seconds across 12 tests in each group</caption><thead><tr><th scope="col">Test</th><th scope="col">Reward with logo</th><th scope="col">Reward separately</th><th scope="col">Learning off</th></tr></thead><tbody>{rows.map(row=><tr key={row.label}><th scope="row">{row.label}</th>{row.dwell.map((v,i)=><td key={i}>{v.toFixed(2)} s</td>)}</tr>)}</tbody></table></div></Disclosure>
 <Disclosure title="Training images and recorded checks"><p>The supplied verification report records checks on all 36 training conditions and 108 walking tests: equal reward doses, unchanged weights when learning was disabled, no reward during tests, and outcomes recalculated from positions. We also checked the imported file hashes and recounted the displayed results from the 108-row measurements file.</p><p>Ten tests ended early when the fly left the observation area; the shortest lasted 2.56 seconds. They remain included. Total recorded walking time was 321.60 seconds.</p><p>These plots show measured training signals and changes, not pleasure or addiction. PAM11 labels the monitored cell group; spikes are electrical pulses; plastic edges are connections whose strength can change.</p>{['instagram','google','whatsapp','youtube'].map(logo=><div key={logo}><h3>{logo==='youtube'?'YouTube':logo==='whatsapp'?'WhatsApp':logo[0].toUpperCase()+logo.slice(1)}</h3>{[101,102,103].map(seed=><figure className="research-figure" key={seed}><a href={`${base}training-plots/training-${logo}-${seed}.png`} target="_blank" rel="noopener noreferrer"><img loading="lazy" src={`${base}training-plots/training-${logo}-${seed}.png`} alt={`${logo} training measurements, starting setup ${seed}`}/></a><figcaption>Starting setup {seed}. Open the image for full-size labels.</figcaption></figure>)}</div>)}</Disclosure>
 <p className="reward-downloads">{[['Full report','report.md'],['All 108 test results','probe-results.csv'],['Method and settings','protocol.json'],['Recorded checks','verification.json'],['File checksums','manifest.json']].map(([label,path])=><a key={path} href={base+path} download>{label}</a>)}</p>
 </div>}
