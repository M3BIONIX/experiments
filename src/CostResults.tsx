import {Disclosure} from './UI';
import data from './cost-results.json';
import './logo.css';
const base='/reports/cost-learning/';
const groups=[['paired','Food linked to logo'],['unpaired','Reward given later'],['frozen','Learning off']] as const;
const stages=[['withdrawal','Food removed'],['after_extinction','Extra viewing without reward'],['high_cost','Higher walking cost'],['punishment','Entry penalty']] as const;
export function CostResults(){return <div className="logo-results reward-results">
 <h3>Reading the chart</h3><p>A bar counts how many times the fly reached its trained logo before another logo, out of 16 chances. Green is food linked to the logo, brown is the same reward given later, and blue is learning switched off. Similar heights mean similar counts. The green bars show no consistent lead.</p>
 <figure className="research-figure"><a href={base+'results.png'} target="_blank" rel="noopener noreferrer"><img src={base+'results.png'} width="1760" height="720" loading="lazy" alt="First approaches out of 16. Food removed: 4, 4, 4. Extra unrewarded viewing: 4, 4, 3. Higher walking cost: 4, 4, 4. Entry penalty: 2, 4, 4."/></a><figcaption>The original chart uses “paired” for food linked to the logo, “unpaired” for reward given later, and “frozen” for learning off. Tap to enlarge.</figcaption></figure>
 <div className="reward-table"><table><caption>Reached the trained logo first</caption><thead><tr><th scope="col">Test</th>{groups.map(([key,name])=><th scope="col" key={key}>{name}</th>)}</tr></thead><tbody>{stages.map(([key,name])=><tr key={key}><th scope="row">{name}</th>{groups.map(([group])=><td key={group}>{data.arms[group].stages[key].trained_first}/16</td>)}</tr>)}</tbody></table></div>
 <h3>The logo-only brain check</h3><p>We watched a group of dopamine-related cells called PAM11. Each group had 16 logo checks and 16 matching blank-panel checks. All totals below are recorded zeros. No reward or cost signal was supplied during these checks.</p>
 <div className="reward-table"><table><caption>Electrical pulses in the monitored cells</caption><thead><tr><th scope="col">Training group</th><th scope="col">Logo shown</th><th scope="col">Blank panel</th></tr></thead><tbody>{groups.map(([key,name])=><tr key={key}><th scope="row">{name}</th><td>{data.arms[key].native_cue_pam11_spikes}</td><td>{data.arms[key].blank_panel_pam11_spikes}</td></tr>)}</tbody></table></div>
 <Disclosure title="Did the fly experience a penalty and return?"><p>A return counted only after a penalty had been fully delivered, the fly had moved at least 4 mm away, and it entered the 2 mm target area again. None of the penalty-exposed trials recorded such a return within the short test.</p><div className="reward-table"><table><caption>Entry-penalty test</caption><thead><tr><th scope="col">Group</th><th scope="col">Received penalty</th><th scope="col">Later returns</th></tr></thead><tbody>{groups.map(([key,name])=><tr key={key}><th scope="row">{name}</th><td>{data.arms[key].stages.punishment.penalty_experienced}/16</td><td>{data.arms[key].stages.punishment.returns_after_delivered_penalty}</td></tr>)}</tbody></table></div></Disclosure>
 <Disclosure title="Did walking cost change the distance travelled?"><p>These are average distances per test. The food-linked group walked slightly less when the cost rose; the delayed-reward group walked slightly more. Learning off produced the same distance. These small differences do not establish learned avoidance.</p><div className="reward-table"><table><caption>Average distance walked</caption><thead><tr><th scope="col">Group</th><th scope="col">Food removed</th><th scope="col">Higher cost</th></tr></thead><tbody>{groups.map(([key,name])=><tr key={key}><th scope="row">{name}</th><td>{(data.arms[key].stages.withdrawal.distance_mm/16).toFixed(2)} mm</td><td>{(data.arms[key].stages.high_cost.distance_mm/16).toFixed(2)} mm</td></tr>)}</tbody></table></div></Disclosure>
 <Disclosure title="Checks on the signals and saved results"><p>In a separate check, the cost input increased activity in the cells receiving it from 30 to 52 pulses. Those cells are labelled PPL101 in the dataset. Connections changed when learning was enabled and stayed unchanged when learning was off. This checks signal delivery and connection changes; it does not establish that the fly learned to avoid a cost.</p><p>The source verification covers all 48 training conditions and 192 walking tests, including the food and cost doses, movement commands, and four target positions. The displayed walking totals were recounted from the saved rows. The copied records match their supplied file checksums.</p><a href={base+'mechanism-checks.json'} download>Signal-delivery checks</a></Disclosure>
 <p className="reward-downloads">{[['Full report','report.md'],['All test outcomes','probes.csv'],['Detailed results','summary.json'],['Recorded checks','verification.json'],['File checksums','manifest.json']].map(([label,path])=><a key={path} href={base+path} download>{label}</a>)}</p>
 </div>}
