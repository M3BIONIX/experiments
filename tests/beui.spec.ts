import {test,expect} from '@playwright/test';
test.use({launchOptions:{executablePath:'/Users/m3bionix/Library/Caches/ms-playwright/chromium_headless_shell-1228/chrome-headless-shell-mac-arm64/chrome-headless-shell',args:['--enable-unsafe-swiftshader']}});
test('beUI navigation, disclosures, select, slider and research figure',async({page})=>{
 const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:5173/#/');await expect(page.locator('h1 a')).toHaveAttribute('href','https://m3bionix.com/');expect(await page.locator('h1 a').evaluate(el=>getComputedStyle(el).textDecorationLine)).toContain('underline');
 await page.getByRole('button',{name:'Fly Logo',exact:true}).click();await page.getByRole('link',{name:'About the experiment',exact:true}).click();await expect(page.locator('h1')).toHaveText('Fly Logo');await expect(page.locator('.research-figure img')).toBeVisible();await page.getByRole('link',{name:'Explore results',exact:true}).click();await expect(page.getByRole('button',{name:'Play recording'})).toBeEnabled();
 await page.getByRole('button',{name:/Trial /}).click();await page.getByRole('option',{name:'003 / 100',exact:true}).click();await expect(page.locator('#download')).toHaveAttribute('href','/reports/neural-logo/trial-003.json');
 await page.getByRole('button',{name:'Play recording'}).click();await expect(page.getByRole('button',{name:'Pause',exact:true})).toBeVisible();const slider=page.getByRole('slider',{name:'Recorded timestep'});await slider.focus();await slider.press('Home');await expect(page.getByRole('button',{name:'Play recording'})).toBeVisible();await expect(slider).toHaveAttribute('aria-valuenow','0');await slider.press('ArrowRight');await expect(slider).toHaveAttribute('aria-valuenow','1');
 await page.getByRole('button',{name:'How were these numbers checked?',exact:true}).click();await expect(page.getByText(/31,458,334 electrical pulses/)).toBeVisible();await page.setViewportSize({width:390,height:844});await page.evaluate(()=>scrollTo(0,0));await page.screenshot({path:'/tmp/beui-replayer-mobile.png',fullPage:true});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);expect(errors).toEqual([]);
});
test('beUI game controls preserve board geometry and moves',async({page})=>{
 await page.goto('http://localhost:5173/#/fly-chess');await expect(page.locator('.game-status')).toContainText('Your turn',{timeout:30000});const bounds=await page.locator('.chessboard').boundingBox();expect(Math.abs(bounds!.width-bounds!.height)).toBeLessThan(2);
 await page.getByRole('button',{name:/^e2 white pawn/}).click();await page.getByRole('button',{name:/^e4 empty/}).click();await expect(page.locator('.game-status')).toContainText('Your turn',{timeout:30000});await page.getByRole('button',{name:'How the fly chooses'}).click();await expect(page.getByText(/Each turn starts from/)).toBeVisible();await page.screenshot({path:'/tmp/beui-chess.png',fullPage:true});
 await page.setViewportSize({width:390,height:844});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);
});
