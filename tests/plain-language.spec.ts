import {test,expect} from '@playwright/test';
test.use({launchOptions:{executablePath:'/Users/m3bionix/Library/Caches/ms-playwright/chromium_headless_shell-1228/chrome-headless-shell-mac-arm64/chrome-headless-shell'}});
test('flat accordion and readable experiment with no automatic sample',async({page})=>{
 const calls:string[]=[];page.on('request',r=>{if(r.url().includes('/api/brain/'))calls.push(r.url())});await page.goto('http://localhost:5173/#/');await page.getByRole('button',{name:'Fly Logo',exact:true}).click();const style=await page.locator('.experiment').first().evaluate(el=>({radius:getComputedStyle(el).borderRadius,bg:getComputedStyle(el).backgroundColor}));expect(style.radius).toBe('0px');expect(style.bg).toBe('rgba(0, 0, 0, 0)');
 await page.getByRole('link',{name:'About the experiment',exact:true}).click();for(const title of ['The question','What we actually tested','What happened','What the results mean','Then we tried adding a reward','What the reward tests found','The final answer'])await expect(page.getByRole('heading',{name:title,exact:true})).toBeAttached();await expect(page.getByText(/No reward was given during these logo tests/)).toBeAttached();await expect(page.getByText(/Their name in the dataset is PAM11/)).toBeAttached();await expect(page.locator('.research-figure img').first()).toHaveAttribute('src','/reports/neural-logo/results-explained.svg');
 for(const id of ['fly-chess','fly-tic-tac-toe']){await page.goto(`http://localhost:5173/#/experiments/${id}`);await expect(page.locator('.article-body')).toBeVisible();await expect(page.locator('.neural-panel')).toHaveCount(0);await expect(page.getByText('Inside the response',{exact:true})).toHaveCount(0);}expect(calls).toEqual([]);
 await page.setViewportSize({width:320,height:740});await page.goto('http://localhost:5173/#/experiments/fly-logo');await expect(page.locator('h1')).toHaveText('Fly Logo');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);
});
